{
  "name": "finai",
  "version": "1.0.0",
  "description": "FinAI - Financial Application for Loan Management",
  "scripts": {
    "build": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader",
    "start": "php -S 0.0.0.0:$PORT -t web",
    "vercel-build": "curl -sS https://getcomposer.org/installer | php && php composer.phar install --no-dev --optimize-autoloader"
  },
  "engines": {
    "php": ">=7.4"
  },
  "keywords": [
    "php",
    "yii2",
    "financial",
    "loans",
    "vercel"
  ],
  "author": "FinAI Team",
  "license": "MIT"
}
